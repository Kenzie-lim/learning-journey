## What I Learned
- pandas
- matplotlib, seaborn

## Key Takeaways

1. **Cell vs Terminal**: Use `%pip` in notebook cells, plain `pip` in terminal
2. **Scalar vs Vector ops**: `and`/`or` for single values, `&`/`|`/`~` for Series
3. **`.agg()`**: Pass multiple functions at once — strings (`'sum'`) for pandas shortcuts, bare names (`my_func`) for custom
